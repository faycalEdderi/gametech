{% extends 'theme.html.twig' %}

{% block title %} {{parent() }} | {{afficher.sujet }} {% endblock %}

{% block body %}
{% include 'inc/forumMenu.html.twig'%} 
            
            <h2 class=" title"> {{afficher.sujet }}</h2>
            <div class="commentaire topicPrincipal">
              <p>De : {{afficher.auteur }}</p>
              <p>{{afficher.message }}</p>
            </div>
<div class="row mt-3">
		<div class="col">
			<div class="comment-list">
					{# affichage des publications #}
					<h3 class="title">Réponses</h3>
				{% for affichage in afficher.publication %}
						<hr>
					<div class="commentaire">
						<p>De : {{ affichage.userName }}</p>
						<p>Réponse: {{ affichage.reponse }}</p>
					<div>
						
				{% endfor %}
				</div>
			<h4>Ajouter un commentaire</h4>
			{% if app.user %}
			<form method="post"  class="form-comment">

			
			

				<p>
				{#Recuperation du prenom de l'utilisateur#}
				<input type="hidden" type="text" name="userName" id="name" value="{{ app.user.prenom }}">
					<textarea class="form-control" name="message" id="message" cols="60" rows="5"></textarea>
				</p>
				<p class=" valider">
					{# champ caché pour stocker l'identifiant du produit #}
					<input type="hidden" name="id" value="{{ afficher.id }}">
					<button  type="submit" class="btn btn-info">Valider</button>
				</p>
			</form>
			{% else %}

			<p> Veuillez vous <a href="{{url('app_login')}}">connecter</a> ou vous <a href="{{url('app_register')}}"> créer un compte </a> pour publier. </p>
			{% endif %}
			
		</div>
	</div>

{% endblock %}

{% block javascripts %}
	<script src="{{ asset('js/forumReponse.js') }}"></script>
{% endblock %}